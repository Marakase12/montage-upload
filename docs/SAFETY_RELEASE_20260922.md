# Выкладка пакета защиты — 2026-09-22

Пользователь разрешил выложить подготовленный пакет защиты.

## Подтверждённая версия

- Финальная версия: `6a2c1c1df172d6b0763769039b935380a6a9f292`, приложение Timeweb **256921**.
  Код защиты вошёл в `92f4175`; затем исправлен текст согласия на хранение.
- Timeweb показал успешный deploy и `В сети`; health возвращает
  `studio-20260922-safety-v2`, `minimumBridgeVersion: 3`, `ownerIsolationVersion: 1`.
- PostgreSQL accountsReady: true; схема осталась `002_account_rate_limits`.
- GitHub Actions: [успешный финальный запуск 35699733313](https://github.com/Marakase12/montage-upload/actions/runs/35699733313).
  `checks / test` и зависимый Pages-deploy завершились успешно.
- Windows: существующая задача `MontageAI Cloud Bridge` запущена; живой процесс
  подтвердил v3, heartbeat и lease support. Два процесса Python — venv launcher
  и его дочерний интерпретатор, а не два независимых worker; lock один.
- Legacy-приложение **256355** по панели остаётся остановленным.

Timeweb обновлён вручную на выбранный коммит; автодеплой не включался.
Все остальные поля формы до сохранения совпадали с исходными.
Порядок ручного деплоя сверен с [официальной инструкцией Timeweb](https://timeweb.cloud/docs/apps/upravlenie-apps-v-paneli).

## Что действительно проверено

| Проверка | Результат |
|---|---|
| Локальные Node-тесты | 114/114 PASS, включая текст согласия |
| Локальные Python-тесты | 72 PASS, 1 необязательный render SKIP |
| Проверка синтаксиса и staged diff | PASS |
| Известные локальные секреты в публикуемых файлах | Не обнаружены; .env/runtime/jobs не публиковались |
| API health / PostgreSQL | 200 / ready |
| Admin auth-check | Новый ключ 200; старый, неверный и отсутствующий 401; no-store |
| Оба worker claim endpoint | Старый/отсутствующий ключ 401; версия 2 — 426 WORKER_UPGRADE_REQUIRED |
| Версия 3 | Проходит version gate; невалидный jobId отклонён 400 без захвата задачи |
| Отчёт хранения | 200, dryRun=true, deletionEnabled=false, обход не усечён |
| Реальная загрузка | Шесть стадий PASS: health/CORS, preflight, тестовый проект, multipart, S3 PUT, complete + список файлов |
| Связь worker | ONLINE; свежий heartbeat; локальная версия 3 |
| Текст хранения на обоих сайтах | Срок доступа отделён от удаления; явно указано, что автоудаление отключено |

Загрузочный smoke использовал небольшой синтетический текстовый файл, не клиентское
видео. Создан только отдельный тестовый проект/объект; облачное удаление не выполнялось.
Отчёт хранилища во время проверки увидел 45 объектов и оставил 2 multipart-загрузки.
Числа — снимок времени, не квота и не обещание состояния после проверки.

## Ограничения и продолжение

- На Windows встречались прерывания TLS-handshake (`UNEXPECTED_EOF_WHILE_READING`)
  и таймауты. Повторные независимые проверки прошли, но стабильность сети этим
  не доказана. Проверка сертификатов не отключалась; worker использует существующий
  backoff. Не маскировать эти сбои увеличением процента готовности.
  Аналогичный TLS-handshake сбой также встретился при git push в GitHub;
  следующий push прошёл. Причина сетевой нестабильности не установлена.
- До запуска worker в очереди был один ранее загруженный ролик. Он не удалялся,
  не заменялся тестом и не подтверждался автоматически. Его готовность проверяется
  отдельно от успешного deployment.
- Полный production-тест двух аккаунтов с видео, скачиванием, правкой «запомни»
  и негативными межпользовательскими обращениями ещё не выполнен. Изоляция покрыта
  локальными тестами, но соответствующие задачи оставлены DEPLOYED, не PROD.
- Длительное отсутствие автоматического удаления не проверялось наблюдением;
  удаляющий код и таймер убраны в развернутой версии, что покрыто тестами.
- Нет новой схемы БД, смены тарифа, ротации ключей, удаления пользовательских
  исходников, нового сервера или Instagram-публикации.

Защищённая локальная резервная копия: `X:\AI\Codex\montage\runtime\safety-deploy-20260922`.
Содержит metadata/preferences, комплект Python и проверенный Git bundle.
Это **не** backup PostgreSQL или всего S3. Старый API из bundle имеет опасный cleanup;
не откатывать его на рабочий bucket без сохранения исправлений защиты.

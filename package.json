{
  "name": "montage-upload-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Chunked large-file uploader for the montage workspace",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "test": "node --test",
    "smoke:cloud": "node scripts/cloud-smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
